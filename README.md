# image-gallery-project
A fully responsive and the gallery displays images in a grid layout. When a user clicks an image, a lightbox popup opens to show a larger view. I also implemented the next and previous navigation using JavaScript. Additionally, I added a filter feature where users can view images based on categories like nature or animals. Tools: HTML, CSS, &amp; JS.
